{
  "name": "app.latexdo.org",
  "version": "0.1.0",
  "private": true,
  "description": "Static download and update metadata site for the LatexDo desktop app.",
  "type": "module",
  "scripts": {
    "build": "npm run ci",
    "ci": "node scripts/validate-downloads-site.mjs",
    "test": "npm run ci"
  }
}
